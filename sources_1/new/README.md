# 原始 Vivado 风格 RTL 备份

本目录保留从原始工程导入的 Vivado 风格 RTL 文件，用于追溯来源和对照阅读。

当前项目的编译入口是项目根目录的 `run.sh`；它只编译 `rtl/` 与 `tb/` 中的文件，不会编译本目录。为了避免同名模块被重复编译、学习时混淆，请优先阅读已补充中文注释的：

```text
rtl/top_looptest.v
rtl/uart_fifo.v
rtl/uart.v
rtl/fifo.v
```

本目录不作为当前学习版的维护对象，保留原始结构和注释是为了方便与开源来源进行逐行对照。
